package _if

import (
	_ "strings"
	_ "allcpptags"
)

func Handle(data *[]string, index int) {
	// expIndex := strings.Index((*data)[index], tags.IF)
	// expression := (*data)[index][expIndex + len(tags.IF) + 1:]

	// return data
}